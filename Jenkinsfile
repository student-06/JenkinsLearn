pipeline {
    agent any

    stages {

        stage('build') {
            steps {
                echo "hello"
                echo "${params.Paramb}"
                echo "${params}"
                
            }
        }
    }
}
