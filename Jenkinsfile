pipeline {
    agent any

    stages {

    stage('Set environment') {
            steps {
                
            }
        }


        stage('build') {
            steps {
                sh "echo hello pnr"
                sh "created dev!"
            }
        }
    }
}
